 <div class="card my-4">
    <h5 class="card-header">Search</h5>
    <div class="card-body">
        
            {{ form_start(showForm, {'action': path('show_find'), 'method': 'POST'}) }}
            <div class="input-group">
            {{ form_widget(showForm.name) }}
            {{ form_errors(showForm.name) }}
            <span class="input-group-btn">
                <input type='submit'class="btn btn-secondary" value="Let's GO">
            </span>
            {{ form_end(showForm ) }}
        </div>
    </div>
</div>
           